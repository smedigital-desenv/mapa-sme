# Copie para ".env" (no MESMO diretório) e preencha. O .env NUNCA vai para o repositório.
# A service_role ignora o RLS — trate como senha. Só na sua máquina.

SUPABASE_URL=https://gmwotfulohkmuqrezeef.supabase.co
SUPABASE_SERVICE_KEY=cole-aqui-a-service-role-do-supabase

# Opcionais:
# SCHEMA=relatorios
# MODELO=claude               # rótulo de quem gerou as devolutivas (aparece na tela p/ comparar)
# API_URL=https://script.google.com/macros/s/SEU_DEPLOY/exec   # web app (só p/ migrar-gemini)
